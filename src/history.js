//history package was installed automatically with react-router-dom
//the history object is a separate library that react-router-dom has as a dependency
import { createBrowserHistory } from 'history'; 


//This will create a history object that we maintain and can use anywhere in this application
export default createBrowserHistory();